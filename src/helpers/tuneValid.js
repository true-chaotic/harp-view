/**
 * Validates tune
 *  - not empty
 *  @TODO: conforms to chosen standard (e.g. abc)
 *
 * @param {string} tune
 * @returns {boolean}
 */
export default function tuneValid(tune) {
  return Boolean(tune);
}
